# syncBoard

Full-stack assignment project.

## Structure

- `client/` — React app (Vite), frontend
- `server/` — backend (coming soon)

## Getting started

```bash
cd client
npm install
npm run dev
```
